import { Node } from "./Node";

export class TreeFactory {
  /**
   * 构造树
   * @param root 根节点数量
   * @param length 每个节点数量
   * @param level 层级深度
   */
  createTree(root: number, length: number, level: number): Node[] {
    return [];
  }
}
